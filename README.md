# Greatest Common Divisor (GCD) 
This project finds the greatest common divisor of two integers entered by the user.It first sorts the numbers,then checks all divisors up to the smaller number.

## What I Learned 
- How to find the GCD using a using a brute force loop
- How to swap two variables using a temporary variable
- How to check divisibility with the modulo operator
